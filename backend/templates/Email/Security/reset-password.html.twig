{% extends 'base.html.twig' %}

{% form_theme form 'Form/fields.html.twig' %}

{% block body_classes %}
    {{ parent() }}
    reset-page
{% endblock %}

{% block main_content %}

    <div class="main-content">
        <a href="{{ path('TechemCoreBundle_TableauBordClient_index') }}" class="logo">
            <img src="{{ asset('bundles/techemcore/images/fidesio/logo.svg') }}"/>
        </a>

        <form action="{{ path('reset_password') }}" method="post" class="form">
            <div class="form-content">
                <h2 class="title">{{ "RÉINITIALISER SON MOT DE PASSE"|trans }}</h2>

                <div class="description"></div>

                {% if not form.vars.valid %}
                    <div class="has-error">
                        <div class="help-block">
                            {{ form_errors(form) }}
                        </div>
                    </div>
                {% elseif success %}
                    <div class="has-success">
                        <div class="help-block">
                            {{ "Mot de passe réinitialisé." }}
                        </div>
                    </div>
                {% endif %}

                {{ form_row(form.email) }}
                {{ form_rest(form) }}
            </div>

            <input type="submit" name="login" value="ENVOYER" class="submit" />
        </form>
    </div>

{% endblock %}
