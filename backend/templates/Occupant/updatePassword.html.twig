{% extends "base_occupant.html.twig" %}

{% form_theme form 'Form/fields.html.twig' %}

{% block body__main_menu %}
    <li class="with-icon">
        <a href="{{ path('TechemCoreBundle_Occupant_show') }}">
            <i class="icon-opened33"></i><span style="" class="title ">{{ "Logement"|trans }} {{ logement.Occupant.Ref
                }}</span>
        </a>
    </li>
    <li class="active">
        <a href="#" class="mdp">
            <span class="title" style="vertical-align: middle">{{ "Modifier votre mot"|trans }} <br>{{ "de passe"|trans }}</span>
        </a>
    </li>
{% endblock %}

{% block breadcrumb_mobile %}
    <div class="btn btn-primary btn-lg dropdown-toggle hidden-lg hidden-md" id="dropdownMenu3" data-toggle="dropdown">
        <i class="pull-right icon-arrow"></i>
        <h3>{{ "Modifier mot de passe"|trans }}</h3>
    </div>
{% endblock %}

{% block main_content %}
    <ul class="breadcrumb hidden-xs">
        <li><a href="#">{{ "Modification mot de passe"|trans }}</a></li>
    </ul>
    <span class="clearfix"></span>

    <div class="header">
        <h2 class="title">{{ "Modification mot de passe"|trans }}</h2>
    </div>

    <div class="row">
        <div class="col-sm-6 operator-view-area">
            {#{% if success is not null %}#}
                {#{% if success %}#}
                    {#<div class="has-success">#}
                        {#<div class="help-block">#}
                            {#Le compte a été créé.#}
                        {#</div>#}
                    {#</div>#}
                {#{% elseif not success %}#}
                    {#<div class="has-error">#}
                        {#<div class="help-block">#}
                            {#Le compte n'a pas pu être créé.#}
                        {#</div>#}
                    {#</div>#}
                {#{% endif %}#}
            {#{% endif %}#}

            {% include 'update-password.html.twig' %}

        </div>
    </div>
{% endblock %}